---
phase: 03-admin-ops-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/FeeStructure.ts
  - src/models/FeeDemand.ts
  - src/services/fee.service.ts
  - src/config/bullmq.ts
  - src/workers/fee.worker.ts
autonomous: true
requirements: [FEE-01, FEE-04, FEE-05]
must_haves:
  truths:
    - "Admin can define a FeeStructure template with multiple line items (e.g., Tuition, Lab, Library)"
    - "System generates FeeDemands for a batch of students based on a structure"
    - "Student demands accurately reflect applied scholarships or concessions (FEE-05)"
    - "Demands are automatically marked as 'Overdue' after their due date passes via background job (FEE-04)"
  artifacts:
    - path: "src/models/FeeStructure.ts"
      provides: "Mongoose schema for reusable fee templates"
    - path: "src/models/FeeDemand.ts"
      provides: "Mongoose schema for student-specific fee records with concession tracking"
    - path: "src/services/fee.service.ts"
      provides: "Business logic for structure management and demand generation"
  key_links:
    - from: "src/services/fee.service.ts"
      to: "src/models/FeeDemand.ts"
      via: "Mongoose find/update/create"
    - from: "src/workers/fee.worker.ts"
      to: "src/services/fee.service.ts"
      via: "checkOverdueDemands method call"
---

<objective>
Establish the foundational data models and business logic for the Fee Management system. This includes defining how fees are structured, how they are demanded from students, and how concessions and late statuses are handled.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-admin-ops-modules/03-RESEARCH.md
@src/models/Student.ts
@src/plugins/mongooseIsolation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Fee Models (Structure and Demand)</name>
  <files>src/models/FeeStructure.ts, src/models/FeeDemand.ts</files>
  <action>
    - Create `FeeStructure` schema: name, academicYear, branch, batch, items (array of { name, amount }).
    - Create `FeeDemand` schema: studentId (ref Student), structureId (ref FeeStructure), items (cloned from structure), concessions (array of { type, amount, reason }), totalAmount, paidAmount, status (Pending, Partial, Paid, Overdue), dueDate.
    - Apply `mongooseIsolation` to both schemas per research (D-02 in Phase 1).
    - Add unique index on FeeDemand (tenantId, studentId, structureId).
  </action>
  <verify>
    Files exist and schemas are exported with isolation plugin applied.
  </verify>
  <done>Models are ready for CRUD operations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FeeService</name>
  <files>src/services/fee.service.ts, tests/fee.service.test.ts</files>
  <behavior>
    - `createStructure`: Saves a new fee structure.
    - `generateDemands`: Takes a structureId and a list of studentIds, creates FeeDemand records for each.
    - `applyConcession`: Updates a FeeDemand with a concession, recalculating the totalAmount.
    - `getStudentBalance`: Aggregates total pending demands for a student.
  </behavior>
  <action>
    Implement FeeService with multi-tenant awareness. Ensure totalAmount in FeeDemand is (sum of items - sum of concessions).
  </action>
  <verify>
    <automated>npm test tests/fee.service.test.ts</automated>
  </verify>
  <done>Business logic for fee management is verified.</done>
</task>

<task type="auto">
  <name>Task 3: Defaulter Tracking Worker</name>
  <files>src/config/bullmq.ts, src/workers/fee.worker.ts</files>
  <action>
    - Add `feeOverdueQueue` to `src/config/bullmq.ts`.
    - Create `src/workers/fee.worker.ts` to process a periodic job that:
      - Finds all `Pending` or `Partial` FeeDemands where `dueDate < now()`.
      - Updates their status to `Overdue`.
    - Setup the BullMQ Worker to listen to the new queue.
  </action>
  <verify>
    Worker is correctly instantiated and status updates can be triggered.
  </verify>
  <done>Automated defaulter tracking is functional (FEE-04).</done>
</task>

</tasks>

<success_criteria>
- FeeStructures can be defined.
- FeeDemands are generated with isolation and correct arithmetic.
- Background worker correctly identifies and marks overdue fees.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-ops-modules/03-01-SUMMARY.md`
</output>
