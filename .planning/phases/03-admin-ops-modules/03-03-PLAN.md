---
phase: 03-admin-ops-modules
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/Faculty.ts
  - src/models/LeaveRequest.ts
  - src/services/hr.service.ts
  - src/routes/hr.routes.ts
autonomous: true
requirements: [HR-01, HR-02, HR-03]
must_haves:
  truths:
    - "Faculty profiles include comprehensive academic and professional history (HR-01)"
    - "Leave requests support multiple types (CL, EL, ML, OD) and hierarchical approvals (HR-02)"
    - "Workload analysis dynamically aggregates data from the Timetable module (HR-03)"
  artifacts:
    - path: "src/models/LeaveRequest.ts"
      provides: "Mongoose schema for leave management with approval workflow history"
    - path: "src/services/hr.service.ts"
      provides: "Business logic for HR operations, leave state transitions, and workload calculation"
  key_links:
    - from: "src/services/hr.service.ts"
      to: "src/models/TimetableEntry.ts"
      via: "Aggregation pipeline for workload calculation"
    - from: "src/models/LeaveRequest.ts"
      to: "src/models/Faculty.ts"
      via: "facultyId reference"
---

<objective>
Implement the Faculty & HR Management module, focusing on detailed professional profiles, a robust leave approval workflow, and automated workload analysis.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-admin-ops-modules/03-RESEARCH.md
@src/models/Faculty.ts
@src/models/TimetableEntry.ts
@src/plugins/mongooseIsolation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Faculty Model and Create LeaveRequest Schema</name>
  <files>src/models/Faculty.ts, src/models/LeaveRequest.ts</files>
  <action>
    - Update `src/models/Faculty.ts`: Ensure it covers all HR-01 requirements (qualifications, publications, experience, contactInfo).
    - Create `src/models/LeaveRequest.ts`:
      - Fields: facultyId (ref Faculty), type (CL, EL, ML, OD), startDate, endDate, reason, status (Pending, Approved, Rejected).
      - Workflow history: approvalChain (array of { approverId, status, comment, timestamp }).
      - Apply `mongooseIsolation` for tenant safety.
  </action>
  <verify>
    <automated>tsx -e "import './src/models/Faculty.ts'; import './src/models/LeaveRequest.ts'; console.log('Models exported successfully')"</automated>
  </verify>
  <done>Data models for HR and Leave management are established.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement HR Service (Leave Workflow & Workload)</name>
  <files>src/services/hr.service.ts, tests/hr.service.test.ts</files>
  <behavior>
    - `requestLeave`: Creates a new LeaveRequest for a faculty member.
    - `updateLeaveStatus`: Handles the approval/rejection logic and appends to the workflow history.
    - `getFacultyWorkload`: Aggregates total weekly hours/slots from `TimetableEntry` for a given facultyId.
  </behavior>
  <action>
    Implement the HR Service. For `getFacultyWorkload`, use a MongoDB aggregation pipeline to count occurrences of facultyId in `TimetableEntry` filtered by the current academic session.
  </action>
  <verify>
    <automated>npm test tests/hr.service.test.ts</automated>
  </verify>
  <done>HR business logic is verified and functional.</done>
</task>

<task type="auto">
  <name>Task 3: HR Routes and RBAC Integration</name>
  <files>src/routes/hr.routes.ts</files>
  <action>
    - Create endpoints:
      - GET `/api/hr/profile`: View own profile (Faculty).
      - PUT `/api/hr/profile`: Update own profile (Faculty).
      - POST `/api/hr/leaves`: Submit leave request.
      - GET `/api/hr/leaves/pending`: List leaves for approval (HOD/Admin roles).
      - PATCH `/api/hr/leaves/:id`: Approve/Reject leave.
      - GET `/api/hr/workload/:facultyId`: View workload (Admin/HOD).
    - Apply `checkRoles` and `checkPermissions` middleware for security.
  </action>
  <verify>
    <automated>npm test tests/hr.routes.test.ts</automated>
  </verify>
  <done>HR functionality is accessible via API with proper security.</done>
</task>

</tasks>

<success_criteria>
- Faculty can manage detailed professional profiles.
- Leave requests can be submitted and processed through an approval chain.
- Faculty workload is automatically calculated based on the master timetable.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-ops-modules/03-03-SUMMARY.md`
</output>
