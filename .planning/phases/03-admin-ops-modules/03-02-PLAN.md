---
phase: 03-admin-ops-modules
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/models/Payment.ts
  - src/services/payment.service.ts
  - src/services/receipt.service.ts
  - src/routes/payment.routes.ts
  - src/schemas/payment.schema.ts
autonomous: true
requirements: [FEE-02, FEE-03]
user_setup:
  - service: razorpay
    why: "Payment gateway integration for online fee collection"
    env_vars:
      - name: RAZORPAY_KEY_ID
        source: "Razorpay Dashboard -> Settings -> API Keys"
      - name: RAZORPAY_KEY_SECRET
        source: "Razorpay Dashboard -> Settings -> API Keys"
      - name: RAZORPAY_WEBHOOK_SECRET
        source: "Razorpay Dashboard -> Settings -> Webhooks"

must_haves:
  truths:
    - "Student can initiate a Razorpay order for a specific FeeDemand"
    - "System captures payment confirmation via webhook and updates the FeeDemand status to 'Paid' or 'Partial'"
    - "System generates a valid PDF receipt including a QR code for verification after a successful payment"
  artifacts:
    - path: "src/models/Payment.ts"
      provides: "Record of payment attempts and successful transactions"
    - path: "src/services/payment.service.ts"
      provides: "Logic for gateway communication and webhook processing"
    - path: "src/services/receipt.service.ts"
      provides: "PDF generation logic with QR code embedding"
  key_links:
    - from: "src/services/payment.service.ts"
      to: "src/models/Payment.ts"
      via: "Mongoose create/update"
    - from: "src/services/payment.service.ts"
      to: "src/models/FeeDemand.ts"
      via: "Mongoose status update"
    - from: "src/services/receipt.service.ts"
      to: "src/models/Payment.ts"
      via: "Data source for receipt details"
---

<objective>
Integrate a payment gateway (Razorpay) to allow online fee payments and implement automated PDF receipt generation with QR code verification. This fulfills requirements for online payment (FEE-02) and automated receipts (FEE-03).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-admin-ops-modules/03-RESEARCH.md
@.planning/phases/03-admin-ops-modules/03-01-SUMMARY.md
@src/models/FeeDemand.ts
@src/models/Student.ts
@src/plugins/mongooseIsolation.ts
</context>

<interfaces>
From src/models/FeeDemand.ts:
```typescript
export interface IFeeDemand {
  _id: string;
  tenantId: string;
  studentId: string;
  structureId: string;
  totalAmount: number;
  paidAmount: number;
  status: 'Pending' | 'Partial' | 'Paid' | 'Overdue';
  // ...
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Add Payment & Receipt Dependencies</name>
  <files>package.json</files>
  <action>
    Add `razorpay` and `qrcode` to dependencies.
    - `razorpay`: ^2.9.6
    - `qrcode`: ^1.5.4
    - `@types/qrcode`: ^1.5.5 (dev)
    Run `npm install`.
  </action>
  <verify>
    <automated>npm list razorpay qrcode</automated>
  </verify>
  <done>External libraries are available for use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Payment Model & Order Initiation</name>
  <files>src/models/Payment.ts, src/services/payment.service.ts, src/routes/payment.routes.ts, src/schemas/payment.schema.ts</files>
  <behavior>
    - `Payment` model stores: tenantId, demandId, studentId, amount, gateway (Razorpay), gatewayOrderId, status (PENDING, SUCCESS, FAILED), transactionId.
    - `paymentService.createOrder(demandId)`:
      - Validates FeeDemand existence and status (not already Paid).
      - Calls Razorpay API to create an order.
      - Saves a PENDING Payment record.
      - Returns the gatewayOrderId to the client.
  </behavior>
  <action>
    - Define `Payment` schema with `mongooseIsolation`.
    - Create `payment.schema.ts` for Fastify route validation.
    - Implement `paymentService.createOrder` using the `razorpay` SDK.
    - Add POST `/api/payments/order` route.
  </action>
  <verify>
    <automated>npm test tests/payment.service.test.ts</automated>
  </verify>
  <done>Orders can be initiated and tracked in the database.</done>
</task>

<task type="auto">
  <name>Task 2: Webhook Verification & Status Sync</name>
  <files>src/services/payment.service.ts, src/routes/payment.routes.ts</files>
  <action>
    - Implement `paymentService.handleWebhook(payload, signature)`:
      - Verifies the signature using `RAZORPAY_WEBHOOK_SECRET`.
      - On `payment.captured`:
        - Finds the corresponding `Payment` record.
        - Updates `Payment` status to SUCCESS and sets `transactionId`.
        - Updates the associated `FeeDemand`: increments `paidAmount` and updates `status` (PAID if totalAmount reached, else PARTIAL).
    - Add POST `/api/payments/webhook` (ensure `fastify-raw-body` is used if needed for signature verification).
  </action>
  <verify>
    <automated>npm test tests/payment.routes.test.ts</automated>
  </verify>
  <done>Payments are verified and fee statuses are synchronized automatically (FEE-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Automated Receipt Generation (PDF + QR)</name>
  <files>src/services/receipt.service.ts, src/routes/receipt.routes.ts</files>
  <behavior>
    - `receiptService.generateReceipt(paymentId)`:
      - Fetches Payment, Student, and FeeDemand details.
      - Generates a QR code containing the Payment ID and a verification URL.
      - Builds a PDF using `pdfmake` with: College Logo/Name (from Tenant), Student Name/ID, Fee Structure items, Amount Paid, Date, Transaction ID, and the QR Code.
      - Returns a Buffer or Stream of the PDF.
  </behavior>
  <action>
    - Implement `receiptService` using `pdfmake` and `qrcode`.
    - Add GET `/api/payments/:paymentId/receipt` route.
    - Ensure the route is protected (only the student or admin can access).
  </action>
  <verify>
    <automated>npm test tests/receipt.service.test.ts</automated>
  </verify>
  <done>Professional PDF receipts with QR codes are generated upon request (FEE-03).</done>
</task>

</tasks>

<success_criteria>
- Razorpay orders are created successfully via the API.
- Webhooks correctly update Payment and FeeDemand statuses.
- PDF receipts are generated with correct data and a scannable QR code.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-ops-modules/03-02-SUMMARY.md`
</output>
