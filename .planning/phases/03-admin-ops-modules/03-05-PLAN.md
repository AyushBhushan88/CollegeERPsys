---
phase: 03-admin-ops-modules
plan: 05
type: execute
wave: 2
depends_on: [03-01, 03-03, 03-04]
files_modified:
  - package.json
  - src/models/Event.ts
  - src/services/calendar.service.ts
  - src/routes/calendar.routes.ts
  - src/services/analytics.service.ts
  - src/routes/admin.routes.ts
autonomous: true
requirements: [COM-02]
must_haves:
  truths:
    - "Admins can create institutional events (e.g. Holidays, Fests) that appear in the calendar"
    - "System provides a valid iCal (.ics) feed that can be imported into external calendar apps"
    - "Admin dashboard displays aggregated metrics for Fees, HR, and Library modules"
  artifacts:
    - path: "src/models/Event.ts"
      provides: "Mongoose schema for institutional calendar events"
    - path: "src/services/calendar.service.ts"
      provides: "Logic for event management and RFC 5545 compliant iCal generation"
    - path: "src/services/analytics.service.ts"
      provides: "Aggregated views of Fee collection, HR status, and Library utilization"
  key_links:
    - from: "src/services/calendar.service.ts"
      to: "ical-generator"
      via: "standard RFC 5545 feed creation"
    - from: "src/services/analytics.service.ts"
      to: "src/models/FeeDemand.ts"
      via: "Mongoose aggregation for collection metrics"
    - from: "src/services/analytics.service.ts"
      to: "src/models/LeaveRequest.ts"
      via: "Mongoose count for pending approvals"
---

<objective>
Implement the Institutional Calendar with iCal feed support and a centralized Admin/Ops Analytics Dashboard. This provides institutional-level visibility and interoperability (COM-02) and aggregates data from Finance, HR, and Library modules.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-admin-ops-modules/03-RESEARCH.md
@src/models/FeeDemand.ts
@src/models/LeaveRequest.ts
@src/models/Circulation.ts
@src/plugins/mongooseIsolation.ts
</context>

<interfaces>
From src/models/FeeDemand.ts (Expected from 03-01):
```typescript
export interface IFeeDemand {
  totalAmount: number;
  paidAmount: number;
  status: 'Pending' | 'Partial' | 'Paid' | 'Overdue';
  tenantId: string;
}
```

From src/models/LeaveRequest.ts (Expected from 03-03):
```typescript
export interface ILeaveRequest {
  status: 'PENDING' | 'APPROVED' | 'REJECTED';
  tenantId: string;
}
```

From src/models/Circulation.ts (Expected from 03-04):
```typescript
export interface ICirculation {
  status: 'issued' | 'returned' | 'overdue';
  tenantId: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Add Calendar Dependencies</name>
  <files>package.json</files>
  <action>
    Add `ical-generator` to dependencies.
    - `ical-generator`: ^10.1.0
    Run `npm install`.
  </action>
  <verify>
    `npm list ical-generator` returns version 10.1.0.
  </verify>
  <done>iCal generation library is available.</done>
</task>

<task type="auto">
  <name>Task 1: Institutional Event Model & Calendar Service</name>
  <files>src/models/Event.ts, src/services/calendar.service.ts, src/routes/calendar.routes.ts</files>
  <action>
    - Create `Event` model: title, description, startDate, endDate, location, type (Holiday, Exam, Event, Academic), tenantId. Apply `mongooseIsolation`.
    - Implement `CalendarService`:
      - Standard CRUD for Events.
      - `generateICalFeed(tenantId)`: Fetches events for the tenant and uses `ical-generator` to create a calendar object. Sets calendar name to "Institutional Calendar".
    - Create `calendar.routes.ts`:
      - `GET /api/calendar/events`: Returns all events for current tenant.
      - `POST /api/calendar/events`: Creates a new event (Admin only).
      - `GET /api/calendar/events/feed.ics`: Returns the generated iCal string with content-type `text/calendar`.
  </action>
  <verify>
    - `curl http://localhost:3000/api/calendar/events` returns JSON array.
    - `curl http://localhost:3000/api/calendar/events/feed.ics` returns valid iCal text.
  </verify>
  <done>Institutional calendar and iCal feed are functional (COM-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admin/Ops Analytics Service</name>
  <files>src/services/analytics.service.ts, tests/analytics.service.test.ts</files>
  <behavior>
    - `getDashboardSummary`:
      - Fees: Aggregates totalDemand (sum), totalCollected (sum), and overdueCount (count where status=Overdue).
      - HR: Counts pendingLeaveRequests (status=PENDING), activeFacultyCount.
      - Library: Counts activeCirculations (status=issued), overdueBooksCount (status=overdue).
  </behavior>
  <action>
    Implement `AnalyticsService` using Mongoose aggregation pipelines. Ensure strict tenant isolation in all queries.
  </action>
  <verify>
    <automated>npm test tests/analytics.service.test.ts</automated>
  </verify>
  <done>Analytics engine provides accurate cross-module insights.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard API Integration</name>
  <files>src/routes/admin.routes.ts</files>
  <action>
    - Import `AnalyticsService`.
    - Add `GET /api/admin/dashboard-summary` route.
    - Protected by `checkRoles(['admin', 'super-admin'])`.
    - Returns the summary object from `analyticsService.getDashboardSummary()`.
  </action>
  <verify>
    `GET /api/admin/dashboard-summary` returns a composite object with fee, hr, and lib fields.
  </verify>
  <done>Admin dashboard API is ready for frontend consumption.</done>
</task>

</tasks>

<success_criteria>
- Institutional events can be managed via API.
- External calendar apps can subscribe to the institutional feed via `.ics` URL.
- Admin dashboard provides a unified view of operational health (Fees, HR, Library).
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-ops-modules/03-05-SUMMARY.md`
</output>
