---
phase: 04-analytics-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/models/ReportDefinition.ts, src/services/reportBuilder.service.ts, src/routes/report.routes.ts, src/schemas/report.schema.ts]
autonomous: true
requirements: [RPT-03]

must_haves:
  truths:
    - "Users can define custom reports with specific columns, filters, and sort orders"
    - "Reports execute dynamic MongoDB aggregation pipelines based on metadata"
    - "Dynamic filters are strictly validated against a whitelist of allowed fields and operators"
  artifacts:
    - path: "src/models/ReportDefinition.ts"
      provides: "Persistence for custom report configurations"
    - path: "src/services/reportBuilder.service.ts"
      provides: "Dynamic pipeline factory with Zod validation"
    - path: "src/routes/report.routes.ts"
      provides: "Endpoints for report CRUD and execution"
  key_links:
    - from: "src/services/reportBuilder.service.ts"
      to: "mongoose models"
      via: "Dynamic model selection based on report metadata"
    - from: "src/routes/report.routes.ts"
      to: "src/services/reportBuilder.service.ts"
      via: "Method calls to execute reports"
---

<objective>
Implement a Custom Report Builder that allows users to create, save, and execute dynamic reports using a metadata-driven aggregation factory with strict Zod validation.

Purpose: To provide flexibility for non-technical users to extract specific datasets without new development.
Output: ReportDefinition model, ReportBuilderService, and corresponding Fastify routes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-analytics-compliance/04-RESEARCH.md
@src/models/Student.ts
@src/models/Faculty.ts
@src/models/Course.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReportDefinition Model and Schemas</name>
  <files>src/models/ReportDefinition.ts, src/schemas/report.schema.ts</files>
  <action>
    1. Create `src/models/ReportDefinition.ts` with the following schema:
       - `name`: string
       - `description`: string
       - `baseModel`: string (e.g., 'Student', 'Faculty', 'FeeDemand')
       - `columns`: Array<{ field: string, label: string, transform?: string }>
       - `filters`: Array<{ field: string, operator: string, value: any }>
       - `sort`: Array<{ field: string, order: 'asc' | 'desc' }>
       - `tenantId`: ObjectId
    2. Create `src/schemas/report.schema.ts` defining Zod schemas for the `ReportDefinition` object and a strict whitelist of allowed fields/operators for each `baseModel`.
  </action>
  <verify>
    <automated>node --check src/models/ReportDefinition.ts</automated>
  </verify>
  <done>Report metadata structure is defined and validated via Zod.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ReportBuilderService Factory</name>
  <files>src/services/reportBuilder.service.ts, tests/reportBuilder.test.ts</files>
  <behavior>
    - Ensure `{ $match: { tenantId } }` is ALWAYS the first stage.
    - Test that only whitelisted operators ($eq, $gt, $lt, $in, $regex) are allowed.
    - Test that the factory generates correct `$project` and `$sort` stages based on metadata.
    - Validate that invalid field names in filters throw a Zod error.
  </behavior>
  <action>
    1. Create `ReportBuilderService` with `buildPipeline(config, tenantId)` method.
    2. Use `mongoose.model(config.baseModel)` to dynamically select the starting collection.
    3. Implement `executeReport(config, tenantId)` which generates the pipeline and returns the result.
    4. Ensure strict type checking to prevent NoSQL injection by validating all input values against the schema.
  </action>
  <verify>
    <automated>npx vitest tests/reportBuilder.test.ts</automated>
  </verify>
  <done>Dynamic aggregation factory is robust, secure, and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Custom Report Routes</name>
  <files>src/routes/report.routes.ts</files>
  <action>
    Create Fastify routes for:
    - `GET /reports`: List saved report definitions.
    - `POST /reports`: Create and save a new report definition.
    - `GET /reports/:id/execute`: Fetch a definition and execute it.
    - `POST /reports/preview`: Execute a report definition without saving it.
    
    Register routes and secure with `checkRole(['admin'])`.
  </action>
  <verify>
    <automated>curl -X POST -H "Content-Type: application/json" -d @test-report.json http://localhost:3000/reports/preview</automated>
  </verify>
  <done>Report builder endpoints are functional and secured.</done>
</task>

</tasks>

<verification>
1. Verify that the dynamic factory correctly translates metadata into valid Mongoose aggregations.
2. Confirm that attempting to use non-whitelisted operators or fields results in validation errors.
3. Test that results are correctly filtered by tenantId even when custom filters are applied.
</verification>

<success_criteria>
- Custom report builder supports dynamic selection of base models and fields.
- Reports can be saved and re-executed.
- Security audit confirms no NoSQL injection vectors in the dynamic filter factory.
</success_criteria>

<output>
After completion, create `.planning/phases/04-analytics-compliance/04-02-SUMMARY.md`
</output>
