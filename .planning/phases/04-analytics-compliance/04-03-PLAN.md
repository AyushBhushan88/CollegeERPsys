---
phase: 04-analytics-compliance
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: [src/services/compliance.service.ts, src/workers/compliance.worker.ts, src/routes/compliance.routes.ts]
autonomous: true
requirements: [RPT-02]

must_haves:
  truths:
    - "Admins can trigger large compliance report generations (NAAC SSR/NBA SAR) in the background"
    - "Users are notified of report status and provided with a secure download link upon completion"
    - "Compliance reports accurately map internal ERP data to official accreditation criteria"
  artifacts:
    - path: "src/services/compliance.service.ts"
      provides: "Mapping logic for NAAC/NBA criteria"
    - path: "src/workers/compliance.worker.ts"
      provides: "BullMQ worker for async XLSX/PDF generation"
    - path: "tests/compliance.worker.test.ts"
      provides: "Integration tests for async report generation"
    - path: "src/routes/compliance.routes.ts"
      provides: "Endpoints for job control and report download"
  key_links:
    - from: "src/services/compliance.service.ts"
      to: "bullmq"
      via: "Job producers for report generation"
    - from: "src/workers/compliance.worker.ts"
      to: "minio"
      via: "File storage of generated reports"
---

<objective>
Build a Compliance Engine to automate the generation of accreditation reports (NAAC SSR and NBA SAR) by extracting and mapping transactional data into official templates using BullMQ and MinIO.

Purpose: To reduce the administrative burden of accreditation cycles and ensure data accuracy.
Output: ComplianceService, BullMQ worker, and async report endpoints.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-analytics-compliance/04-RESEARCH.md
@src/services/analytics.service.ts
@src/services/reportBuilder.service.ts
@src/config/bullmq.ts
@src/config/minio.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ComplianceService with Criteria Mapping</name>
  <files>src/services/compliance.service.ts</files>
  <action>
    Create `ComplianceService` with methods to extract data for key NAAC/NBA criteria:
    1. `extractNaacData(tenantId, criteria)`: Specifically handle Criterion 2 (Student performance/SFR) and Criterion 5 (Scholarships/Placements).
    2. `extractNbaData(tenantId, criteria)`: Handle CO-PO attainment and success rates.
    3. `enqueueReportJob(tenantId, reportType, filters)`: Pushes a job to the `compliance-generation` queue.
    
    Use `AnalyticsService` for core metrics and the `ReportBuilder` for specific data extracts where applicable.
  </action>
  <verify>
    <automated>node --check src/services/compliance.service.ts</automated>
  </verify>
  <done>ComplianceService can orchestrate data extraction and job queuing.</done>
</task>

<task type="auto">
  <name>Task 2: Develop Compliance Worker for Async Generation</name>
  <files>src/workers/compliance.worker.ts, tests/compliance.worker.test.ts</files>
  <action>
    1. Create `tests/compliance.worker.test.ts` to verify async report generation and storage.
    2. Create a BullMQ worker in `src/workers/compliance.worker.ts` to process `compliance-generation` jobs:
       - Fetch criteria data via `ComplianceService`.
       - Use `exceljs` to generate multi-sheet XLSX workbooks for NAAC/NBA SSR datasets.
       - Use `pdfmake` to generate high-fidelity SAR reports for NBA criteria.
       - Upload generated files to MinIO in the `compliance-reports` bucket.
       - Update job progress and store the MinIO object key in a temporary Redis record for retrieval.
  </action>
  <verify>
    <automated>npm test tests/compliance.worker.test.ts</automated>
  </verify>
  <done>Worker successfully generates and stores large compliance reports in the background.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Compliance Routes and Status Tracking</name>
  <files>src/routes/compliance.routes.ts</files>
  <action>
    Create endpoints for compliance management:
    - `POST /compliance/generate`: Triggers a report job; returns `jobId`.
    - `GET /compliance/status/:jobId`: Returns current generation status (queued, processing, completed, failed).
    - `GET /compliance/download/:jobId`: Generates a signed MinIO URL for the report if completed.
    
    Secure endpoints with `checkRole(['admin'])`. Ensure users can only download reports belonging to their tenant.
  </action>
  <verify>
    <automated>curl -X POST -H "Authorization: Bearer $TOKEN" -d '{"type":"NAAC_SSR"}' http://localhost:3000/compliance/generate</automated>
  </verify>
  <done>Admins can trigger, track, and download compliance reports via the API.</done>
</task>

</tasks>

<verification>
1. Trigger a NAAC report generation and verify that it appears in the BullMQ queue (via Redis check or logs).
2. Confirm that the generated file is successfully uploaded to MinIO and contains real data mappings.
3. Test that the signed URL correctly expires and is secured by tenantId.
</verification>

<success_criteria>
- Background generation works for large datasets without blocking the main process.
- Reports accurately reflect the mapping specified in Phase 4 research.
- All file operations are isolated and secured within the multi-tenant architecture.
</success_criteria>

<output>
After completion, create `.planning/phases/04-analytics-compliance/04-03-SUMMARY.md`
</output>
