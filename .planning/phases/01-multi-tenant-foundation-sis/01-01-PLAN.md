---
phase: 01-multi-tenant-foundation-sis
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified: [
  "package.json",
  "src/index.ts",
  "src/app.ts",
  "src/config/db.ts",
  "src/config/redis.ts",
  "src/models/Tenant.ts",
  "src/models/User.ts",
  "src/middleware/tenant.ts",
  "src/plugins/mongooseIsolation.ts"
]
autonomous: true
requirements: [AUTH-06, COM-01, COM-03]
user_setup:
  - service: mongodb
    why: "Primary database for multi-tenant data"
    env_vars:
      - name: MONGODB_URI
        source: "Local or Atlas MongoDB Connection String"
  - service: redis
    why: "Caching and session management"
    env_vars:
      - name: REDIS_URL
        source: "Redis connection string (e.g., redis://localhost:6379)"

must_haves:
  truths:
    - "Fastify server starts without error"
    - "MongoDB and Redis connections are established"
    - "Tenant ID is successfully captured in AsyncLocalStorage from request headers"
    - "Mongoose queries automatically append tenantId filter"
  artifacts:
    - path: "src/plugins/mongooseIsolation.ts"
      provides: "Global Mongoose plugin for query filtering"
    - path: "src/middleware/tenant.ts"
      provides: "Fastify hook for AsyncLocalStorage context"
  key_links:
    - from: "src/middleware/tenant.ts"
      to: "src/plugins/mongooseIsolation.ts"
      via: "AsyncLocalStorage context"
---

<objective>
Setup the core infrastructure for the backend, including Fastify, MongoDB, and Redis, and implement the multi-tenant isolation layer using AsyncLocalStorage and a Mongoose plugin.

Purpose: Ensure data isolation at the database level to prevent cross-tenant data leakage.
Output: A working Fastify server with tenant-aware database queries.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Fastify & MongoDB/Redis Infrastructure</name>
  <files>package.json, src/index.ts, src/app.ts, src/config/db.ts, src/config/redis.ts, src/models/Tenant.ts, src/models/User.ts</files>
  <action>
    - Initialize Node.js project with TypeScript.
    - Install dependencies: fastify, mongoose, redis, zod, @fastify/env.
    - Setup Fastify server in app.ts.
    - Configure Mongoose connection in src/config/db.ts.
    - Configure Redis connection in src/config/redis.ts.
    - Create base Tenant model with fields: name, slug, settings.
    - Create base User model with fields: email, password, roles, tenantId (ref: Tenant).
  </action>
  <verify>
    <automated>npm run dev (should start and connect to DB/Redis)</automated>
  </verify>
  <done>Backend infrastructure is running and connected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Multi-tenant Isolation Layer</name>
  <files>src/middleware/tenant.ts, src/plugins/mongooseIsolation.ts</files>
  <behavior>
    - Middleware should extract 'x-tenant-id' from header and store in AsyncLocalStorage.
    - Mongoose plugin should intercept 'find', 'findOne', 'update', 'delete' etc. and add '{ tenantId }' to the query filter.
    - It should throw an error if tenantId is missing from context for sensitive operations.
  </behavior>
  <action>
    - Create src/middleware/tenant.ts using node's AsyncLocalStorage.
    - Create src/plugins/mongooseIsolation.ts as a global Mongoose plugin.
    - Apply the plugin to all schemas in src/config/db.ts.
  </action>
  <verify>
    <automated>npm test (unit test verifying that User.find() without filter results in a query with tenantId)</automated>
  </verify>
  <done>All database queries are automatically scoped by tenant context.</done>
</task>

</tasks>

<verification>
- Verify that a request with 'x-tenant-id' header results in a database query with that ID.
- Verify that a request without 'x-tenant-id' results in an error or default behavior (as configured).
</verification>

<success_criteria>
- Fastify server is operational.
- Tenant isolation is enforced at the Mongoose level.
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-tenant-foundation-sis/01-01-SUMMARY.md`
</output>
