---
phase: 01-multi-tenant-foundation-sis
plan: 01-06
type: execute
wave: 5
depends_on: ["01-02", "01-03", "01-04"]
files_modified: [
  "src/app/layout.tsx",
  "src/app/page.tsx",
  "src/app/auth/login/page.tsx",
  "src/app/dashboard/layout.tsx",
  "src/components/auth/AuthContext.tsx",
  "src/lib/api.ts"
]
autonomous: true
requirements: [AUTH-02, SIS-01, SIS-02]
user_setup: []

must_haves:
  truths:
    - "Users can log in through a multi-tenant-aware login page"
    - "Role-based dashboards (Admin, Faculty, Student) are correctly displayed"
    - "Authenticated users see their personal profile details"
    - "Authenticated users can upload/view documents in the Evidence Vault"
  artifacts:
    - path: "src/components/auth/AuthContext.tsx"
      provides: "Global Auth state and multi-tenant context management"
    - path: "src/app/dashboard/layout.tsx"
      provides: "Role-based sidebar and navigation"
  key_links:
    - from: "src/components/auth/AuthContext.tsx"
      to: "Backend /auth/login"
      via: "Fetch / Axios API call"
---

<objective>
Setup the Next.js frontend foundation with multi-tenant login, role-based layouts, and basic profile views.

Purpose: Provide a user-facing interface for the multi-tenant system with role-specific experiences.
Output: Working Next.js application with Auth and SIS UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-multi-tenant-foundation-sis/01-02-SUMMARY.md
@.planning/phases/01-multi-tenant-foundation-sis/01-03-SUMMARY.md
@.planning/phases/01-multi-tenant-foundation-sis/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js Setup & Multi-tenant Auth Context</name>
  <files>package.json, src/app/layout.tsx, src/components/auth/AuthContext.tsx, src/app/auth/login/page.tsx, src/lib/api.ts</files>
  <action>
    - Initialize Next.js 14 project with TypeScript and Tailwind CSS.
    - Create AuthContext with login, logout, and token persistence (cookies/localStorage).
    - Setup Axios/Fetch interceptor in src/lib/api.ts to include 'x-tenant-id' and 'Authorization' headers.
    - Build Login page that accepts Tenant Slug and User credentials.
  </action>
  <verify>
    <automated>npm run dev (login page should render and submit successfully)</automated>
  </verify>
  <done>Frontend project setup with Auth and multi-tenant context.</done>
</task>

<task type="auto">
  <name>Task 2: Role-based Dashboards & Profile Views</name>
  <files>src/app/dashboard/layout.tsx, src/app/dashboard/admin/page.tsx, src/app/dashboard/student/page.tsx, src/app/dashboard/faculty/page.tsx, src/components/profile/ProfileCard.tsx, src/components/vault/DocumentList.tsx</files>
  <action>
    - Implement a role-based navigation sidebar in dashboard/layout.tsx.
    - Create basic dashboards for Admin, Student, and Faculty roles.
    - Build Profile views fetching data from SIS API.
    - Implement a document list/upload component for the Evidence Vault.
  </action>
  <verify>
    <automated>npm test (E2E test logging in as different roles and verifying dashboard views)</automated>
  </verify>
  <done>Dashboards and profile views are operational for all roles.</done>
</task>

</tasks>

<verification>
- Verify that logging in with a Student account redirects to the Student dashboard.
- Verify that profile details shown belong to the logged-in user.
- Verify that file uploads from the UI appear in the Evidence Vault backend.
</verification>

<success_criteria>
- Frontend is fully integrated with the multi-tenant backend.
- Role-based experiences are correctly delivered.
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-tenant-foundation-sis/01-06-SUMMARY.md`
</output>
