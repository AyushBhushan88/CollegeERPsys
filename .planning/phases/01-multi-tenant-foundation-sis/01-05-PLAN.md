---
phase: 01-multi-tenant-foundation-sis
plan: 01-05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified: [
  "src/services/import.service.ts",
  "src/routes/import.routes.ts",
  "src/schemas/import.schema.ts"
]
autonomous: true
requirements: [SIS-04]
user_setup: []

must_haves:
  truths:
    - "Admins can upload a CSV or Excel file containing student records"
    - "System validates data integrity (e.g., email uniqueness, required fields) before importing"
    - "Successfully validated records are created as Student profiles and User accounts"
    - "A summary report (success/fail count + error details) is provided for each bulk import"
  artifacts:
    - path: "src/services/import.service.ts"
      provides: "Bulk student import and validation logic"
  key_links:
    - from: "src/routes/import.routes.ts"
      to: "src/services/import.service.ts"
      via: "Fastify Route Handler"
---

<objective>
Implement a bulk import service for Student records using CSV and Excel formats.

Purpose: Enable rapid onboarding of thousands of student profiles with automated validation.
Output: Working Bulk Import service with record validation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-multi-tenant-foundation-sis/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bulk Student Import & Validation</name>
  <files>src/services/import.service.ts, src/routes/import.routes.ts, src/schemas/import.schema.ts</files>
  <behavior>
    - Parse CSV/Excel file and validate data against Student/User schemas.
    - Create Student profile and User account for each valid row.
    - All created records must be automatically assigned the current tenantId.
    - Provide a detailed error report for invalid rows.
  </behavior>
  <action>
    - Use libraries like 'csv-parse' or 'exceljs' to parse files.
    - Use Zod to validate each row.
    - Implement a transactional or batch-safe import loop in import.service.ts.
    - Create a POST /sis/import endpoint for file upload.
  </action>
  <verify>
    <automated>npm test (integration test for bulk import with 1,000+ records and intentionally invalid data)</automated>
  </verify>
  <done>Bulk student import is functional with robust validation.</done>
</task>

</tasks>

<verification>
- Verify that a file with 1,000 records is processed within a reasonable time (< 30s).
- Verify that duplicate emails or missing fields are correctly flagged and rejected.
- Verify that all imported students belong to the correct tenant.
</verification>

<success_criteria>
- Bulk import is operational for large datasets.
- Data integrity is maintained through multi-stage validation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-tenant-foundation-sis/01-05-SUMMARY.md`
</output>
