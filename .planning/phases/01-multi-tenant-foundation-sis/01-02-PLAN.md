---
phase: 01-multi-tenant-foundation-sis
plan: 01-02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "src/services/auth.service.ts",
  "src/routes/auth.routes.ts",
  "src/middleware/rbac.ts",
  "src/schemas/auth.schema.ts"
]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]
user_setup: []

must_haves:
  truths:
    - "Super Admin can register a new Tenant and its Admin user"
    - "Users can log in and receive JWT access/refresh tokens"
    - "RBAC middleware allows/denies access based on user role and permissions"
    - "Access tokens are validated and include tenantId context"
  artifacts:
    - path: "src/services/auth.service.ts"
      provides: "JWT and User session management"
    - path: "src/middleware/rbac.ts"
      provides: "Role-Based Access Control logic"
  key_links:
    - from: "src/routes/auth.routes.ts"
      to: "src/services/auth.service.ts"
      via: "Fastify Route Handler"
---

<objective>
Implement multi-tenant registration, role-based authentication, and JWT session management.

Purpose: Provide secure entry to the system with automated role-based dashboard redirection and data isolation.
Output: Working Auth and RBAC service.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-multi-tenant-foundation-sis/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Multi-tenant Registration & JWT Logic</name>
  <files>src/services/auth.service.ts, src/routes/auth.routes.ts, src/schemas/auth.schema.ts</files>
  <behavior>
    - Super Admin can create Tenant + Admin user in one transaction.
    - Admin user can create Faculty/Student users for their tenant.
    - Login should verify credentials and return { accessToken, refreshToken, user }.
    - Refresh token can be used to generate new access token.
  </behavior>
  <action>
    - Create Auth schemas with Zod for validation.
    - Implement auth services: registerTenant, login, refreshToken, revokeSession.
    - Setup Fastify routes for /auth/register and /auth/login.
    - Use @fastify/jwt for token generation and signing.
  </action>
  <verify>
    <automated>npm test (integration tests for register/login/refresh)</automated>
  </verify>
  <done>Authentication service is functional for all roles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RBAC & Middleware Enforcement</name>
  <files>src/middleware/rbac.ts, src/routes/admin.routes.ts</files>
  <behavior>
    - Middleware should check user's roles against required roles/permissions.
    - Deny access with 403 if user lacks authorization.
    - Allow access with 200/201 if authorized.
  </behavior>
  <action>
    - Define Role and Permission sets.
    - Implement rbac middleware (checkRoles).
    - Protect administrative routes using this middleware.
  </action>
  <verify>
    <automated>npm test (unit test verifying that Faculty cannot access Super Admin routes)</automated>
  </verify>
  <done>RBAC is correctly enforced across API routes.</done>
</task>

</tasks>

<verification>
- Verify that registration adds users to the correct tenant.
- Verify that a JWT token from Tenant A cannot access Tenant B resources.
</verification>

<success_criteria>
- Multi-tenant auth is secure and operational.
- JWT tokens are used for all protected routes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-tenant-foundation-sis/01-02-SUMMARY.md`
</output>
