---
phase: 01-multi-tenant-foundation-sis
plan: 01-04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: [
  "src/services/vault.service.ts",
  "src/routes/vault.routes.ts",
  "src/models/Evidence.ts",
  "src/config/minio.ts"
]
autonomous: true
requirements: [SIS-02]
user_setup:
  - service: minio
    why: "Secure storage for documents and evidence"
    env_vars:
      - name: MINIO_ENDPOINT
        source: "MinIO Server Endpoint (e.g., localhost)"
      - name: MINIO_PORT
        source: "MinIO Port (e.g., 9000)"
      - name: MINIO_ACCESS_KEY
        source: "MinIO User Access Key"
      - name: MINIO_SECRET_KEY
        source: "MinIO User Secret Key"

must_haves:
  truths:
    - "Documents can be uploaded to MinIO and retrieved via API"
    - "Each upload is associated with a specific tenant and student profile"
    - "Metadata for uploaded documents is stored in MongoDB"
    - "MinIO buckets are created/managed per tenant (or logically partitioned)"
  artifacts:
    - path: "src/services/vault.service.ts"
      provides: "MinIO file management and metadata tracking"
    - path: "src/config/minio.ts"
      provides: "MinIO client configuration"
  key_links:
    - from: "src/services/vault.service.ts"
      to: "minio-sdk"
      via: "API call"
---

<objective>
Implement the Evidence Vault using MinIO for secure document storage.

Purpose: Provide a scalable and isolated document storage system for student marksheets, photos, and institutional evidence.
Output: Working Evidence Vault service with MinIO integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-multi-tenant-foundation-sis/01-01-SUMMARY.md
@.planning/phases/01-multi-tenant-foundation-sis/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MinIO Integration & Metadata Tracking</name>
  <files>src/config/minio.ts, src/services/vault.service.ts, src/models/Evidence.ts, src/routes/vault.routes.ts</files>
  <behavior>
    - Setup MinIO client in config/minio.ts.
    - Implement uploadDocument: Accepts a file, uploads to MinIO with a tenant-based path, and saves metadata in MongoDB (path, mime, size, studentId).
    - Implement getDocumentById: Returns a signed URL or stream for document retrieval.
    - Implement deleteDocument: Removes from both MinIO and MongoDB.
    - All operations must be automatically scoped by tenantId.
  </behavior>
  <action>
    - Initialize MinIO client.
    - Define Evidence metadata model.
    - Create vault service and routes for file upload/download.
    - Use @fastify/multipart for handling file uploads.
  </action>
  <verify>
    <automated>npm test (integration tests for file upload/retrieval from MinIO)</automated>
  </verify>
  <done>MinIO integration and evidence tracking are functional.</done>
</task>

</tasks>

<verification>
- Verify that uploaded documents are stored in the correct MinIO bucket/prefix for the tenant.
- Verify that metadata is correctly stored and searchable in MongoDB.
- Verify that a user from Tenant A cannot access documents from Tenant B.
</verification>

<success_criteria>
- Evidence Vault is operational and isolated.
- Large files (marksheets/photos) are handled correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-tenant-foundation-sis/01-04-SUMMARY.md`
</output>
