---
phase: 02-academic-core-obe
plan: 03
type: execute
wave: 3
depends_on: [02-01]
files_modified:
  - src/models/Room.ts
  - src/models/TimetableEntry.ts
  - src/models/Substitution.ts
  - src/services/tt.service.ts
  - src/routes/tt.routes.ts
  - src/schemas/tt.schema.ts
  - src/app/dashboard/admin/timetable/page.tsx
  - src/app/dashboard/faculty/timetable/page.tsx
  - src/app/dashboard/student/timetable/page.tsx
  - tests/tt.service.test.ts
autonomous: true
requirements: TT-01, TT-02, TT-03

must_haves:
  truths:
    - "System prevents scheduling a room or faculty for two concurrent classes (TT-01)"
    - "Admins can view a master grid with real-time conflict validation (TT-01)"
    - "Substitution system suggests free faculty for a specific time slot (TT-02)"
    - "Students, Faculty, and Admins have role-specific timetable dashboards (TT-03)"
  artifacts:
    - path: "src/models/TimetableEntry.ts"
      provides: "Timetable schema with room/faculty/slot constraints and compound indexes"
    - path: "src/services/tt.service.ts"
      provides: "Core logic for scheduling, conflict detection, and substitution suggestions"
    - path: "src/app/dashboard/admin/timetable/page.tsx"
      provides: "Validation-first interface for master scheduling"
  key_links:
    - from: "src/models/TimetableEntry.ts"
      to: "MongoDB"
      via: "Unique compound indexes on (roomId, slot) and (facultyId, slot)"
    - from: "src/services/tt.service.ts"
      to: "src/models/Substitution.ts"
      via: "Mongoose ODM for tracking faculty replacements"
---

<objective>
Implement the Timetable & Scheduling system (TT), ensuring conflict-free master scheduling, effective substitution management, and role-based personalized views.

Purpose: Automate the complex process of institutional scheduling while preventing resource conflicts and maintaining high faculty utilization.
Output: Integrated Timetable models with DB-level constraints, substitution management service, and personalized timetable dashboards for all stakeholders.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/models/Faculty.ts
@src/models/Course.ts
@src/models/CourseRegistration.ts
@src/plugins/mongooseIsolation.ts
</context>

<interfaces>
From src/models/Faculty.ts:
```typescript
export interface IFaculty {
  _id: string;
  userId: string;
  department: string;
  tenantId: string;
}
```

From src/models/Course.ts (from 02-01):
```typescript
export interface ICourse {
  _id: string;
  code: string;
  name: string;
  tenantId: string;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Room & Timetable Infrastructure (TT-01)</name>
  <files>src/models/Room.ts, src/models/TimetableEntry.ts, src/services/tt.service.ts, src/routes/tt.routes.ts, src/schemas/tt.schema.ts, tests/tt.service.test.ts</files>
  <behavior>
    - Create a Room with name, type (Theory/Lab), capacity, and tenantId.
    - Create a TimetableEntry linked to Room, Faculty, Course, and Section for a specific Day (1-6) and Slot (1-8).
    - DB Constraint 1: Prevent same Room + Day + Slot overlap (Room Conflict).
    - DB Constraint 2: Prevent same Faculty + Day + Slot overlap (Faculty Conflict).
    - DB Constraint 3: Prevent same Section + Day + Slot overlap (Class Conflict).
    - Ensure all models are multi-tenant isolated via `mongooseIsolation` plugin.
  </behavior>
  <action>
    1. Define `Room` model: { tenantId, name, type (enum), capacity, location }.
    2. Define `TimetableEntry` model: { tenantId, dayOfWeek (1-6), slotIndex (1-8), roomId, facultyId, courseId, sectionId, batchId (optional for labs) }.
    3. Add 3 unique compound indexes to `TimetableEntry` to enforce the constraints listed in "behavior".
    4. Implement `tt.service.ts`: `createRoom`, `createTimetableEntry` (with check for duplicate/conflict before saving), `getTimetableForRoom`.
    5. Register routes in `tt.routes.ts` with Zod validation.
    6. Write tests in `tests/tt.service.test.ts` to verify conflict prevention (should throw MongoServerError on duplicate index).
  </action>
  <verify>
    <automated>npm test tests/tt.service.test.ts</automated>
  </verify>
  <done>Infrastructure for conflict-free scheduling is implemented with DB-level constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Substitution Management & Availability Logic (TT-02)</name>
  <files>src/models/Substitution.ts, src/services/tt.service.ts, src/routes/tt.routes.ts</files>
  <behavior>
    - Identify "Free Faculty" for a specific Day and Slot (faculty who do NOT have a TimetableEntry for that slot).
    - Suggest substitutes primarily from the same department.
    - Create a Substitution record linking an existing TimetableEntry to a new substitute faculty for a specific date.
    - Ensure substitution does not cause a conflict for the substitute faculty.
  </behavior>
  <action>
    1. Define `Substitution` model: { tenantId, entryId (TimetableEntry), date (Date), substituteId (Faculty), status (Pending/Approved), requestedBy }.
    2. Implement `getFreeFaculty(day, slot, department?)` in `tt.service.ts`:
       - Query `Faculty` list.
       - Filter out faculty who have a `TimetableEntry` or an existing `Substitution` (as substitute) for the given slot.
    3. Implement `createSubstitutionRequest` with validation to ensure the substitute is actually free.
    4. Add endpoints for substitution management to `tt.routes.ts`.
    5. Write tests to verify that a faculty marked for substitution cannot be assigned another overlapping task.
  </action>
  <verify>
    <automated>npm test tests/tt.service.test.ts</automated>
  </verify>
  <done>Substitution system is functional with free-faculty discovery and conflict validation.</done>
</task>

<task type="auto">
  <name>Task 3: Personalized Views & Master Scheduling UI (TT-03)</name>
  <files>src/app/dashboard/admin/timetable/page.tsx, src/app/dashboard/faculty/timetable/page.tsx, src/app/dashboard/student/timetable/page.tsx</files>
  <action>
    1. Implement `src/app/dashboard/admin/timetable/page.tsx`:
       - A master grid (Rooms vs. Time Slots).
       - Drag-and-drop or modal-based scheduling.
       - Highlight conflicts if the backend returns a 409 error.
    2. Implement `src/app/dashboard/faculty/timetable/page.tsx`:
       - Personalized weekly view showing their own classes and substitutions.
       - Button to "Request Substitution" for a specific slot.
    3. Implement `src/app/dashboard/student/timetable/page.tsx`:
       - Weekly view filtered by their section and batch (uses `CourseRegistration` to identify courses).
    4. Ensure all views are responsive and use Tailwind CSS for the grid layout.
  </action>
  <verify>
    <automated>curl -X GET http://localhost:3000/api/tt/my-timetable (mocked with appropriate role headers)</automated>
  </verify>
  <done>Stakeholder-specific timetable views are functional with scheduling UI.</done>
</task>

</tasks>

<verification>
1. Multi-tenant isolation is verified across Room, TimetableEntry, and Substitution models.
2. Conflict detection is enforced at the database level via compound unique indexes.
3. Substitution logic correctly excludes busy faculty.
4. UI accurately reflects combined data from regular entries and temporary substitutions.
</verification>

<success_criteria>
- Admin is physically unable to double-book a room or faculty (DB error).
- Faculty can see their own schedule correctly including substitutions.
- Student view only shows classes relevant to their section/batch.
- API response for a weekly timetable remains < 500ms even with 100+ entries.
</success_criteria>

<output>
After completion, create `.planning/phases/02-academic-core-obe/02-03-SUMMARY.md`
</output>
