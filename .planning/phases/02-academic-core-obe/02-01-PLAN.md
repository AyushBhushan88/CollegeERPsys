---
phase: 02-academic-core-obe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/Course.ts
  - src/models/Syllabus.ts
  - src/models/Outcome.ts
  - src/models/OBEMapping.ts
  - src/models/CourseRegistration.ts
  - src/services/crs.service.ts
  - src/routes/crs.routes.ts
  - src/schemas/crs.schema.ts
  - src/app/dashboard/crs/catalog/page.tsx
  - src/app/dashboard/crs/obe/page.tsx
  - src/app/dashboard/crs/register/page.tsx
  - tests/crs.service.test.ts
autonomous: true
requirements: [CRS-01, CRS-02, CRS-03, CRS-04]

must_haves:
  truths:
    - "Admins can create courses with credit definitions and prerequisites (CRS-01)"
    - "Syllabus changes are version-controlled with unit-wise breakdown (CRS-02)"
    - "CO-PO/PSO mapping matrices use a normalized MongoDB structure (CRS-03)"
    - "Students can view and register for elective/open courses (CRS-04)"
  artifacts:
    - path: "src/models/Course.ts"
      provides: "Course schema with credit and type fields"
    - path: "src/models/OBEMapping.ts"
      provides: "Normalized mapping between COs and POs/PSOs"
    - path: "src/services/crs.service.ts"
      provides: "Core logic for catalog, versioning, and mapping"
    - path: "src/routes/crs.routes.ts"
      provides: "Endpoints for all CRS operations"
  key_links:
    - from: "src/routes/crs.routes.ts"
      to: "src/services/crs.service.ts"
      via: "Fastify route handlers"
    - from: "src/services/crs.service.ts"
      to: "src/models/Course.ts"
      via: "Mongoose ODM with tenant isolation"
---

<objective>
Implement the Course & Curriculum Management system and the Outcome-Based Education (OBE) mapping engine.

Purpose: Enable academic administration (catalog/syllabus), ensure accreditation compliance (OBE mapping), and facilitate student registration for electives.
Output: Integrated CRS backend services, normalized OBE mapping engine, and student registration portal.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/middleware/tenant.ts
@src/plugins/mongooseIsolation.ts
@src/schemas/sis.schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Course Catalog & Syllabus Management (CRS-01, CRS-02)</name>
  <files>src/models/Course.ts, src/models/Syllabus.ts, src/services/crs.service.ts, src/routes/crs.routes.ts, src/schemas/crs.schema.ts, tests/crs.service.test.ts</files>
  <behavior>
    - Create a course with code, name, credits, type (core/elective/open), and prerequisites.
    - Add a syllabus version to a course with multiple units (title, description).
    - Ensure subsequent syllabus updates increment version and do not overwrite the old version.
    - Verify that courses are isolated by tenantId via the mongooseIsolation plugin.
  </behavior>
  <action>
    1. Define `Course` model with `type` (enum), `credits` (number), `prerequisites` (array of course references), and `tenantId`.
    2. Define `Syllabus` model with `courseId`, `version` (integer), `units` (array of objects), `status` (draft/active/archived), and `tenantId`.
    3. Implement `crs.service.ts` with `createCourse`, `getCourseCatalog`, and `updateSyllabus` (handle version increment).
    4. Register routes in `crs.routes.ts` with Zod validation.
    5. Write tests in `tests/crs.service.test.ts` to verify catalog CRUD and syllabus versioning.
  </action>
  <verify>
    <automated>npm test tests/crs.service.test.ts</automated>
  </verify>
  <done>Admins can manage a versioned course catalog with prerequisite logic and tenant isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OBE Engine - CO-PO/PSO Mapping (CRS-03)</name>
  <files>src/models/Outcome.ts, src/models/OBEMapping.ts, src/services/crs.service.ts, src/routes/crs.routes.ts, tests/crs.service.test.ts</files>
  <behavior>
    - Define Program Outcomes (PO) and Program Specific Outcomes (PSO) at the department/college level.
    - Define Course Outcomes (CO) for specific courses.
    - Map a CO to a PO/PSO with a weight (1: Slight, 2: Moderate, 3: Substantial).
    - Ensure mappings use a normalized collection (one document per mapping) as per technical requirement.
    - Retrieve a mapping matrix for a course.
  </behavior>
  <action>
    1. Define `Outcome` model with `type` (CO/PO/PSO), `code`, `description`, `courseId` (optional, for CO), and `tenantId`.
    2. Define `OBEMapping` model with `sourceId` (Outcome), `targetId` (Outcome), `weight` (Number 0-3), and `tenantId`.
    3. Implement mapping logic in `crs.service.ts`: `createOutcome`, `createMapping`, and `getMappingMatrix`.
    4. Add mapping endpoints to `crs.routes.ts`.
    5. Extend tests to verify CO-PO mapping integrity and matrix retrieval.
  </action>
  <verify>
    <automated>npm test tests/crs.service.test.ts</automated>
  </verify>
  <done>OBE Engine supports normalized mapping of COs to POs/PSOs with weightage tracking.</done>
</task>

<task type="auto">
  <name>Task 3: Course Registration Portal (CRS-04)</name>
  <files>src/models/CourseRegistration.ts, src/services/crs.service.ts, src/app/dashboard/crs/catalog/page.tsx, src/app/dashboard/crs/register/page.tsx, src/app/dashboard/crs/obe/page.tsx</files>
  <action>
    1. Define `CourseRegistration` model: { studentId, courseId, semester, status, tenantId }.
    2. Implement `registerStudentForCourse` in `crs.service.ts` with checks for prerequisites.
    3. Create `src/app/dashboard/crs/catalog/page.tsx` showing the list of courses with their syllabi.
    4. Create `src/app/dashboard/crs/register/page.tsx` allowing students to filter electives/open courses and register.
    5. Create `src/app/dashboard/crs/obe/page.tsx` showing a read-only matrix of CO-PO mappings for selected courses.
  </action>
  <verify>
    <automated>curl -X GET http://localhost:3000/api/crs/courses</automated>
  </verify>
  <done>Student registration portal is functional with prerequisite validation and elective selection.</done>
</task>

</tasks>

<verification>
1. All CRS requirements (CRS-01 to CRS-04) are addressed in the service and route logic.
2. Tenant isolation is verified by creating a course in one tenant and ensuring it's invisible to another.
3. Syllabus versioning prevents data loss during curriculum updates.
4. OBE Mapping follows the requested normalized MongoDB structure.
</verification>

<success_criteria>
- Admin can successfully create and version a syllabus.
- CO-PO mapping matrix returns correctly weighted values.
- Student can register for an elective course only if prerequisites are met.
- Multi-tenant isolation is strictly maintained across all new collections.
</success_criteria>

<output>
After completion, create `.planning/phases/02-academic-core-obe/02-01-SUMMARY.md`
</output>
