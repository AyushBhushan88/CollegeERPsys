---
phase: 02-academic-core-obe
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - package.json
  - src/models/AttendanceRecord.ts
  - src/models/Condonation.ts
  - src/services/att.service.ts
  - src/routes/att.routes.ts
  - src/schemas/att.schema.ts
  - src/workers/att.worker.ts
  - src/config/bullmq.ts
  - src/app/dashboard/faculty/attendance/page.tsx
  - src/app/dashboard/student/attendance/page.tsx
  - tests/att.service.test.ts
autonomous: true
requirements: [ATT-01, ATT-02, ATT-03, ATT-04, ATT-05]
user_setup:
  - service: redis
    why: "Required for BullMQ job queue"
    env_vars:
      - name: REDIS_URL
        source: "Local redis or managed instance"

must_haves:
  truths:
    - "Faculty can mark daily attendance for a specific course and session (ATT-02)"
    - "Attendance records are strictly isolated by tenantId and indexed for performance (ATT-01)"
    - "Students see a progress-based dashboard showing their current attendance percentage (ATT-03)"
    - "System triggers a background notification when a student's attendance falls below 75% (ATT-04)"
    - "Students can submit condonation requests which, when approved, are reflected in reports (ATT-05)"
  artifacts:
    - path: "src/models/AttendanceRecord.ts"
      provides: "Schema for individual attendance entries with slot and status"
    - path: "src/workers/att.worker.ts"
      provides: "BullMQ worker for processing shortage notifications"
    - path: "src/services/att.service.ts"
      provides: "Core logic for marking, shortage calculation, and report generation"
    - path: "src/config/bullmq.ts"
      provides: "Queue and worker configuration for background tasks"
  key_links:
    - from: "src/services/att.service.ts"
      to: "src/workers/att.worker.ts"
      via: "BullMQ Queue.add()"
    - from: "src/app/dashboard/student/attendance/page.tsx"
      to: "src/services/att.service.ts"
      via: "GET /api/att/stats"
---

<objective>
Implement the Attendance Management System (ATT), including manual marking, shortage tracking, background notifications, and condonation workflows.

Purpose: Automate institutional attendance tracking and ensure students maintain the mandatory 75% requirement.
Output: Integrated attendance marking interface, background shortage engine, and condonation approval system.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/models/Course.ts
@src/models/Student.ts
@src/config/redis.ts
@src/plugins/mongooseIsolation.ts
</context>

<interfaces>
From src/models/Course.ts (from 02-01):
```typescript
export interface ICourse {
  _id: string;
  code: string;
  name: string;
  tenantId: string;
}
```

From src/models/Student.ts:
```typescript
export interface IStudent {
  _id: string;
  userId: string;
  rollNumber: string;
  tenantId: string;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core Attendance Marking & Infrastructure (ATT-01, ATT-02)</name>
  <files>package.json, src/models/AttendanceRecord.ts, src/services/att.service.ts, src/routes/att.routes.ts, src/schemas/att.schema.ts, tests/att.service.test.ts, src/app/dashboard/faculty/attendance/page.tsx</files>
  <behavior>
    - Create an AttendanceRecord for multiple students in a single request (bulk marking).
    - Ensure a compound index on (tenantId, studentId, courseId, date) exists for O(1) lookups during daily marking.
    - Prevent marking attendance for the same student/course/date/slot more than once.
    - Faculty UI should fetch the list of students registered for the course (using CourseRegistration from 02-01).
  </behavior>
  <action>
    1. Install dependencies: `npm install bullmq pdfmake @types/pdfmake`.
    2. Define `AttendanceRecord` model: { studentId, courseId, date, slotId, status (Present/Absent/Late/Excused), markedBy, tenantId }.
    3. Implement `att.service.ts`: `markAttendance` (bulk create), `getRegisterList`.
    4. Register routes in `att.routes.ts` with RBAC (faculty/admin only for marking).
    5. Create `src/app/dashboard/faculty/attendance/page.tsx`: A grid-based marking interface with "Select All" default.
    6. Write tests in `tests/att.service.test.ts` to verify bulk marking and index constraints.
  </action>
  <verify>
    <automated>npm test tests/att.service.test.ts</automated>
  </verify>
  <done>Attendance marking core is functional with tenant isolation and faculty UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dashboard, Reports & Condonation Workflow (ATT-03, ATT-05)</name>
  <files>src/models/Condonation.ts, src/services/att.service.ts, src/routes/att.routes.ts, src/app/dashboard/student/attendance/page.tsx</files>
  <behavior>
    - Calculate cumulative attendance percentage for a student in a course.
    - Submit a condonation request with evidence (medical/event).
    - Approval of condonation should increment the "Present" count (or separate count) in report calculations.
    - Generate an Excel report of monthly attendance for a course using `exceljs`.
  </behavior>
  <action>
    1. Define `Condonation` model: { studentId, courseId, startDate, endDate, type, evidenceUrl, status (Pending/Approved/Rejected), tenantId }.
    2. Implement `att.service.ts`: `getStudentStats`, `submitCondonation`, `approveCondonation`, `generateCourseReport`.
    3. Update `att.routes.ts` with condonation and report endpoints.
    4. Create `src/app/dashboard/student/attendance/page.tsx`: Circular progress bars for attendance % per course and condonation status list.
    5. Write tests to verify percentage calculation accuracy including condoned sessions.
  </action>
  <verify>
    <automated>npm test tests/att.service.test.ts</automated>
  </verify>
  <done>Student dashboard and condonation workflows are implemented with reporting capabilities.</done>
</task>

<task type="auto">
  <name>Task 3: Background Shortage Notifications (ATT-04)</name>
  <files>src/config/bullmq.ts, src/workers/att.worker.ts, src/services/att.service.ts</files>
  <action>
    1. Create `src/config/bullmq.ts` to initialize Queue and Worker using existing Redis client.
    2. Define `ShortageWorker`: 
       - Fetches all students in a course.
       - Calculates percentage.
       - If < 75%, logs a notification (to be integrated with actual email/SMS in Phase 3).
    3. In `att.service.ts`, after `markAttendance` (or via a daily cron), add a job to the `shortage-check` queue.
    4. Ensure worker is tenant-aware (passes tenantId in job data).
  </action>
  <verify>
    <automated># Manual check via BullMQ dashboard (if enabled) or logs showing job processing</automated>
  </verify>
  <done>Automated background engine detects attendance shortage and triggers notifications.</done>
</task>

</tasks>

<verification>
1. Attendance marking is isolated by tenantId.
2. Shortage calculation correctly handles condoned sessions.
3. Background jobs are queued and processed asynchronously via BullMQ.
4. Excel/PDF reports are generated without blocking the main event loop (Task 2/3).
</verification>

<success_criteria>
- Faculty can mark attendance for 60+ students in < 2 seconds.
- Student dashboard accurately reflects percentage drops.
- Notifications are queued only for students strictly below the 75% threshold.
- Condonation requests require Admin approval to affect percentages.
</success_criteria>

<output>
After completion, create `.planning/phases/02-academic-core-obe/02-02-SUMMARY.md`
</output>
